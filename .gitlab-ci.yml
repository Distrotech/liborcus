image: gcc

build:
  stage: build

  before_script: 
    - apt update && apt -y install make autoconf automake libtool libboost1.58-dev
  script: 
    - ./autogen.sh && make

test:
  stage: test
  script:
    - make check
